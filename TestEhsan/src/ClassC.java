public abstract class ClassC implements TestInterface {

}
/*
 * when we implement a class to an interface we have to choose: first, we must
 * override all of interface's methods or second, we must make class abstract
 * and then all of class's child have to override method of interface and parent
 * both
 */